
先概述一下暑假所尝试的新的领域：
1. Agent 全流程学习
2. 黑客松比赛
3. 自驾

其中第二个领域在 [[Review of my first hackthon——AIx Origin Summit]] 有专文复述，这里主要复盘 agent 学习的部分，并简要提及自驾的部分。

### Agent 学习

起因是学长提及了 agent 开发，相对于纯后端而言更加吃香，不过一个暑假学习下来，我对 agent 开发有了一些新的想法。

先复盘一下学习的过程吧。

一开始试图直接看 pi 的文档，发现脱离代码其实很难理解具体的设计，所以就决定选一个 harness 开始看。正好使用 pi 比较多，之前没有用过 opencode、hermes 等开源 harness，且 codex 和 cc 又是闭源的情况下，那就从 pi 开始吧。

不过说实话如此规模的程序想凭借自己的能力看完，我还是没有这个能力，应该也没有这个必要。所以我从 github 上 clone 了 pi 的源码，就在 pi 的帮助下开始了苦苦钻研——从 agent loop，function call 到 event stream、context compaction 等等一共完成了7个 stage 的学习，详情可以参考 [allwayso/learn-pi-agent: Learning Pi agent from no where](https://github.com/allwayso/learn-pi-agent/tree/main)。

现在看来，这种类似挖空练习的方式效率有些低下，其中一些章节没有单独开的必要，不过更大的问题是，一开始希望设计一个层层累进的模式，但是由于缺乏对 agent 整体架构的理解，或是因为某些模块本身太大而做了简化，导致前期设计的模块没有给后续模块留下接口，使得后期不断需要修改前面的地基，甚至单开章节做整合（比如从 agent 到 fullAgent 再到 AgentHarness）。

但是这个学习过程还是有价值的，对 agent 基本上所有部分都有涉及，并且由于是通过挖空的形式来完成，逼迫自己看完源码再工作，多少也能加深对具体实现的理解。

现在看来，其实应该从类似 [shareAI-lab/learn-claude-code: Bash is all you need - A nano claude code–like 「agent harness」, built from 0 to 1](https://github.com/shareAI-lab/learn-claude-code) 这种较为成熟、完整的教程开始出发，完成通读之后再去回头看源码。

不过在完成了 learn-pi-agent 的设计之后，我也意识到了不足，需要通过其他资料来做补充，所以后面就转向了基于 lcc 的专题展开学习，比如 [[memory？memory！]] 和 [[Permission in claude code]] 等等，在学习的过程中发现，agent 设计的模式其实不尽相同，这不仅仅是指功能上的实现方向不同，在设计理念上也不同——这点在拓展方式上尤其不同，比如 pi、cc、dsh 的拓展方式，是完全不同的设计理念。

总的而言，往后新领域的学习可以参考以下模式：
1. 成熟资料：首先参考较新、较为完整资料，不求理解，草草浏览一遍也有帮助
2. 源码收集：将源码 clone 到本地对后续细节展开很有帮助
3. 熟练使用：先不管内部实现，用起来再说，这里主要是指依赖 sdk 等方式进行架构
4. 模块展开：从不同的几个模块展开细节，这一块要深入源码

当然这个模式也还不是很完善，比如源码可以看作成熟资料的一部分，尤其是其中的文档和 commit 记录是很有价值的资料。还有一点，discord 等社区的资源我暂时还没有怎么用上，不过人的力量应该也是很重要的一部分。

开头时说对 agent 开发的新的想法，主要是以下几点：
1. agent 开发本质上也是后端开发，但是在信息交互的速度上要求比较高
2. harness 没有单独开发的必要，而且单人之力不可及
3. agent 开发主要是指在 harness 的基础上做 extension，完善 harness 的功能

### 自驾回顾

在平潭岛上尝试了第一次自驾，三天两晚，总的开销为租车费175+油费159。

和打车比起来，自驾还是要方便许多，这主要体现在可以在任意时间去到任意的地方，当然驾驶本身也是一种乐趣（至少现在对于我而言还是有趣的），而且开销其实差不太多（如果用电车的话）。

不过说实话最好还是有至少两个人换着开，这样可以轮流休息，不至于由于精力不足造成危险。



